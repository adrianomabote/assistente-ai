services:
  - type: web
    name: zapmcrm
    runtime: node
    region: frankfurt

    buildCommand: npm ci && npm run build

    startCommand: npm start
    healthCheckPath: /api/status

    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_PASSWORD
        generateValue: true
      - key: DATA_DIR
        value: /data

    disk:
      name: zapmcrm-data
      mountPath: /data
      sizeGB: 1
